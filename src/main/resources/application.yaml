spring:
  application:
    name: notification-engine

  datasource:
    url: jdbc:mysql://localhost:3306/notification_engine
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
  redis:
    host: localhost
    port: 6379

ratelimit:
  per-minute:
    10